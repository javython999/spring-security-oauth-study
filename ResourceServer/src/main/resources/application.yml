spring:
  application:
    name: OAuth2ResourceServer
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          introspection-uri: http://localhost:9000/oauth2/intropect
          client-id: oauth2-clinet-app1
          client-secret: secret1
#        jwt:
#          jwk-set-uri: http://localhost:8080/realms/oauth2/protocol/openid-connect/certs
server:
  port: 8082